import react from 'react';

const Navbar = () => {
    return (
        <nav className='flex space-x-6'>
            <a href="#">HOME</a>
            <a href="#">BOOK</a>
            <a href="#">CONTACT</a>
        </nav>
    );
}

export default Navbar;